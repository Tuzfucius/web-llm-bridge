# Plugin Tests

Python tests cover review protocol parsing, Git prompt collection, repo-scoped
state, fake Bridge sessions, retry/unknown-delivery behavior, and Hook schema
decisions. The Node fixture test exercises the preserved experimental runtime
without a browser or network connection.
